var n = 10;

struct Person {
  age: int,
  name: string
}

var x = #Person{ age: 5, name: "somehting" };

@fibonacci(current, next, count) {
  if count {
    print current;
    fibonacci(next, current + next, count - 1);
  } else {
    return 0;
  }
}

var start = if n > 0 {
  fibonacci(0, 1, n);
} else {
  print "Invalid input";
};

print x;

