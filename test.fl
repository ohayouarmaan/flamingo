var n = 10;

struct Person {
  age: int,
  name: string,
  father: Person
}

var father = #Person{ age: 32, name: "Something's dad", father: nil };
var x = #Person{ age: 5, name: "somehting", father: father };

@fibonacci(current, next, count) {
  if count {
    print current;
    fibonacci(next, current + next, count - 1);
  } else {
    return 0;
  }
}


print "father's father age is " + x.father.age;

